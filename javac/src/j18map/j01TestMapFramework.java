package j18map;

public class j01TestMapFramework {
	
	
	// Map (Interface)
	// SortedMap (Interface) extends Map (Interface)

    // HashMap & SahTable (Class) implements Map (Interface)
	// TreeMap (Class) implements SortedMap (Interface)
    // LinkedHashMap (Class) implement Map (Interact)

    // Map object are in key-value pair
    // Map object can hold by duplicate value but not by Key (Key always unique)
    // Allow one null as key but null as value allow duplicate
    // Each key-value (pair) consider as a Entry (Entry is an interface)
    // Map.Entry is used to access or represent a pir (key-value)
    // HashMap - not maintain order
    // LinkedHashMap - maintain order
    // TreeMap - sort order by comparator

    // keySet() - return the key of collection
    // values() - return the values of collection
    // keySet() - return the map.entry set
    // entrySet() - return a set collection entry

    // get(key) - return value of the key
    // getValue() - return a key of the value
    // containsKey(key) - return boolean
    // containsValue(value) - return boolean
    // put (k,v) - add object onto map
    // put (map m) - add a map collection on another
    // is empty() - return boolean
    // replace(k, v , new v) - update value
    // remove (key) - remove key-value


}
