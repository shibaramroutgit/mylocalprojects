package j18collection.j01collections;

public class j01TestCollectionsFramework {
	
	
	
	// Iterator (Interface) 
	// Collections (Interface) extends Iterator
	// List (Interface), Queue(Interface) and Set (Interface) extends Collections (Interface)
	
	//1 List (Interface)
	// ArrayList and Vector (Class) Implements List (Interface)
	
	//2 Queue (Interface)
	// Priority Queue (Class) Implements Queue (Interface)
	
	//LinkedList (Class) Implements List & Queue (Interface)
	
	//3.1 Set (Interface)
	//3.2 SortedSet (Interface) extends Set (Interface)
	// HashSet and LinkedHashSet (Class) implements Set (Interface)
	//TreeSet (Class) implements SortedSet (Interface)
	
	
	
	
	
	

}
