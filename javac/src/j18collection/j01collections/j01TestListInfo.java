package j18collection.j01collections;

public class j01TestListInfo {
	
	// List (Interface) implements by ArrayList, LinkedList and Vector (Class)
	// Object store by index value, so the order we store in same order we can get the value using its index
	// Duplicate and duplicate null allow to store 
	// When we add object into a index where object present then new object added into the index and existing all objected from that index shifted to right 
	// When we remove object into a index then all objected from that index shifted to left
	
	
	

}
