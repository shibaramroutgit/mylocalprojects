package j18collection.j01collections;

public class j01TestSetInfo {
	
	// Set (Interface) implements by HashSet and Linked HashSet (class)
	// SortedSet (Interface) extends Set (Interface) and it implements by TreeSet (Class)
	// No Duplicate allow , not even duplicate null
	// Insert Order not persevered so we cannot access by index 
	
	// HashSet object order by its HashCode
	// TreeSet object order by Comparator
	// LinkedHashSet maintain insert order
	
	

}
