package j11arrays;

public class j00TestArrayInfo {
	
	// NegativeArraySizeException , Array length cannot be negative if we do we get 
	// Size of array always integer, if we add (10.25) it throw compile error
	// Declaration and instantiating of an array strictly must be of same type, else compile error
	// 
}
