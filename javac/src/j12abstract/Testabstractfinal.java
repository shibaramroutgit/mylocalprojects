package j12abstract;

public  abstract class Testabstractfinal {

	// if we declare class as "public final abstract class GrandFather" it throw compile error 
	// It is because final class not allow to modify where abstract allow to override in sub-class, both are opposite so it allow at a time
	
	public static void main(String[] args) {
		

	}

}
